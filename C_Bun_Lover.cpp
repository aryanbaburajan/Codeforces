#include <bits/stdc++.h>
using namespace std;

void solve()
{
    long long n;
    cin >> n;

    cout << (n + 1) * (n + 1) + 1 << endl;
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t = 2;
    cin >> t;

    while (t--)
        solve();
    return 0;
}